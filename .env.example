# Copy this to `.env` and fill in real values for local development.
# Never commit real production credentials to the repository.

MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=portfolio
DEFAULT_ADMIN_PASSWORD=ChangeMeToAStrongPassword

# If deploying to Vercel, add these as Project Environment Variables (not in repo)
# VERCEL_TOKEN=...
# VERCEL_PROJECT_ID=...